"""Unit checks for the ecology's own modules, over the declared placeholder ramp.

The claims about what the substrate does at real bar boundaries are evidence and
live beside the approved fixtures, in the consumer repository. What lives here is
the other half: that the module behaves as its own contract says on inputs this
repository can construct — an incomplete row, a break in the sequence, a
declaration deeper than the run will hold.
"""
